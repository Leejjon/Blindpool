import 'mocha'
import * as sinon from 'sinon';
import { Datastore } from '@google-cloud/datastore';

describe('BlindpoolStorageService tests', () => {
    describe('TEST', () => {
        // let stub: sinon.SinonStub<any[], any>;
        // beforeEach(() => {
        //     stub = sinon.stub(Datastore, 'Datastore()').returns('hello');
        // });
        //
        // afterEach(() => {
        //     stub.restore();
        // });
        //
        // it('test', async () => {
        //     someFunction();
        //     expect(stub.calledOnce).to.equal(true);
        // });
    });
});